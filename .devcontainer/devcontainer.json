{
  "name": "Snowdream Tech Universal Workspace",
  "image": "ghcr.io/snowdreamtech/devcontainers:debian",
  "remoteUser": "root",
  "forwardPorts": [],
  "mounts": [
    "source=${localEnv:HOME},target=/host-home,type=bind,consistency=cached,readonly",
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "containerEnv": {
    "ENABLE_GITHUB_PROXY": "0",
    "MISE_QUIET": "0",
    "MISE_IGNORE_MISE_TOML": "1",
    "MISE_WARN_UNTRUSTED": "0",
    "HOST_HOME": "/host-home"
  },
  "postCreateCommand": "VERBOSE=1 sh ${containerWorkspaceFolder}/.devcontainer/init.sh 2>&1 | tee /tmp/devcontainer-init.log",
  "postStartCommand": "echo '✅ Devcontainer is ready!'"
}
